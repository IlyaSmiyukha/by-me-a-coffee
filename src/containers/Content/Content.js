import styled from "styled-components";

const ContentContainer = styled.section`
`;

const  Content = () => {
  return (
    <ContentContainer>

    </ContentContainer>
  );
}

export default Content;
